pipeline:
  build:
    image: golang:1.9.2
    commands:
      - cd /var/cache/drone/src/github.com/Appscrunch/Multy-back
      - make all-with-deps
  publish:
    image: plugins/docker
    repo: multyio/backend:1.0.2
branches: [ drone-test, master]

deploy-staging:
  image: rics3n/drone-ansible:2
  inventory: staging
  secrets: [ ssh_key ]
  when:
    branch: master

notify:  
  email:
    recipients:
      - isitiriss@gmail.com